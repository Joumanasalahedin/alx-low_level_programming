#include "main.h"

/**
  * _abs - computes the absolute value of an integer
  *
  * @int: integar
  *
  * Return: absolute value
  */

int _abs(int)
{
	if (int < 0)
	{
		int = 0 - int;
		_putchar(int);
	}

	else if (int > 0)
	{
		_putchar(int);
	}

	else if (int == 0)
	{
		_putchar('0');
	}
	return (0);
}
